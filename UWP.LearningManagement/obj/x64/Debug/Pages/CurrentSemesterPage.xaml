<Page
    x:Class="UWP.LearningManagement.CurrentSemesterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ListBox x:ConnectionId='2' x:Name="CoursesListBox"
                ItemsSource="{Binding FinalGrades}" Margin="10,0,10,0">
        <ListBox.ItemTemplate>

            <DataTemplate>
                <TextBlock Grid.Column="0">
                    <Run Text="{Binding Path=Key.Name, Mode=OneWay}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding Path=Key.Code, Mode=OneWay}"/>
                    <Run Text="      "/>
                    <Run Text="{Binding Path=Value, Mode=OneWay}"/>
                    <Run Text="%"/>
                </TextBlock>
            </DataTemplate>
        </ListBox.ItemTemplate>
    </ListBox>
</Page>

