<ContentDialog x:ConnectionId='1'
    x:Class="UWP.LearningManagement.Dialogs.AssignmentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Make a new Assignment"
    PrimaryButtonText="Add"
    SecondaryButtonText="Cancel"
                                                         
                                                             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="10,10,10,10" Grid.Column="0" Text="Name"></TextBlock>
        <TextBox Margin="10,10,10,10" Grid.Column="1" Text="{Binding Path=Assignment.Name, Mode=TwoWay}"></TextBox>
        <TextBlock Margin="10,10,10,10"  Grid.Row="1" Text="Description"></TextBlock>
        <TextBox Margin="10,10,10,10" Grid.Row="1" Grid.Column="1" Text="{Binding Path=Assignment.Description, Mode=TwoWay}"></TextBox>
        <TextBlock Margin="10,10,10,10" Grid.Row="2" Text="Total Points"></TextBlock>
        <TextBox Margin="10,10,10,10" Grid.Row="2" Grid.Column="1" Text="{Binding Path=TotalPoints, Mode=TwoWay}"></TextBox>


        <TextBlock Margin="10,10,10,10" Grid.Row="3" Grid.Column="0" Text="Assignment&#x0a;Group"></TextBlock>
        <ComboBox Margin="10,10,10,10" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Stretch" ItemsSource="{Binding AssignmentGroups}" SelectedItem="{Binding SelectedGroup, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Display, Mode=OneWay}"></TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <TextBlock Margin="10,10,10,10" Grid.Row="4" Grid.Column="0" Text="Module"></TextBlock>
        <ComboBox Margin="10,10,10,10" Grid.Row="4" Grid.Column="1" HorizontalAlignment="Stretch" ItemsSource="{Binding Modules}" SelectedItem="{Binding SelectedModule, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding AssignDisplay, Mode=OneWay}"></TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
            <TextBlock Margin="10,10,10,10" Grid.Row="5" Grid.Column="0" Text="Due Date"></TextBlock>
        <DatePicker Margin="10,10,10,10" Grid.Row="5" Grid.Column="1" SelectedDate="{Binding Path=DueDate, Mode=TwoWay}"/>
    </Grid>
</ContentDialog>

