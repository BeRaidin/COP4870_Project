<ContentDialog x:ConnectionId='1'
    x:Class="UWP.LearningManagement.Dialogs.PersonDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Add new Student"
    PrimaryButtonText="Ok"
    SecondaryButtonText="Cancel"
                                                         
                                                             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Name"></TextBlock>
        <TextBox Grid.Column="1" Text="{Binding Path=Name, Mode=TwoWay}"></TextBox>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Type"></TextBlock>
        <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding PersonTypes}" SelectedItem="{Binding SelectedType, Mode=TwoWay}" />
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Grade"></TextBlock>
        <ComboBox Grid.Row="4" Grid.Column="1" ItemsSource="{Binding StudentLevels}" SelectedItem="{Binding SelectedLevel, Mode=TwoWay}" />
        
    </Grid>
</ContentDialog>

