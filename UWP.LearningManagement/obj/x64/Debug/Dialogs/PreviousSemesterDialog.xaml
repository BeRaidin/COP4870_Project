<ContentDialog x:ConnectionId='1'
    x:Class="UWP.LearningManagement.Dialogs.PreviousSemesterDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.LearningManagement.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{Binding SemesterTitle}"
    PrimaryButtonText="Ok"
                                                         >

    <Grid>
        <ListBox ItemsSource="{Binding Courses}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Grid.Column="0">
                    <Run Text="{Binding Path=Key.Name, Mode=OneWay}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding Path=Key.Code, Mode=OneWay}"/>
                    <Run Text="      "/>
                    <Run Text="{Binding Path=Value, Mode=OneWay}"/>
                    <Run Text="%"/>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</ContentDialog>

